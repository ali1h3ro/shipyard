#!/usr/bin/env bash
set -euo pipefail

PROJECT="${1:-}"
DOMAIN="_"

shift || true

while [ $# -gt 0 ]; do
  case "$1" in
    --domain)
      DOMAIN="${2:-}"
      shift 2
      ;;
    *)
      echo "Unknown option: $1"
      exit 1
      ;;
  esac
done

if [ -z "$PROJECT" ]; then
  echo "Usage: shipyard create <project> [--domain example.com]"
  exit 1
fi

if [[ ! "$PROJECT" =~ ^[a-zA-Z0-9_-]+$ ]]; then
  echo "Invalid project name. Use only letters, numbers, hyphen, and underscore."
  exit 1
fi

PROJECT_DIR="/var/www/sites/$PROJECT"
CONFIG_FILE="/etc/deploy-sites/$PROJECT.conf"
NGINX_AVAILABLE="/etc/nginx/sites-available/$PROJECT"
NGINX_ENABLED="/etc/nginx/sites-enabled/$PROJECT"
TEMPLATE="/opt/shipyard/templates/nginx/static.conf"

if [ -e "$PROJECT_DIR" ]; then
  echo "Project directory already exists: $PROJECT_DIR"
  exit 1
fi

if [ -e "$CONFIG_FILE" ]; then
  echo "Config already exists: $CONFIG_FILE"
  exit 1
fi

mkdir -p "$PROJECT_DIR/releases"
mkdir -p "$PROJECT_DIR/shared"

cat > "$CONFIG_FILE" <<EOF
PROJECT_NAME="$PROJECT"
PROJECT_DIR="$PROJECT_DIR"
KEEP_RELEASES=5
TYPE="static"
HEALTH_CHECK_FILE="index.html"
DOMAIN="$DOMAIN"
EOF

sed \
  -e "s|{{PROJECT_NAME}}|$PROJECT|g" \
  -e "s|{{PROJECT_DIR}}|$PROJECT_DIR|g" \
  -e "s|{{DOMAIN}}|$DOMAIN|g" \
  "$TEMPLATE" > "$NGINX_AVAILABLE"

if [ ! -L "$NGINX_ENABLED" ]; then
  ln -s "$NGINX_AVAILABLE" "$NGINX_ENABLED"
fi

nginx -t
sudo systemctl reload nginx

echo "Project created: $PROJECT"
echo "Directory: $PROJECT_DIR"
echo "Config: $CONFIG_FILE"
echo "Nginx: $NGINX_AVAILABLE"
echo "Domain: $DOMAIN"
